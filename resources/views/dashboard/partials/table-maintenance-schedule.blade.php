<div data-table-wrapper><h3 class="font-semibold text-navy mb-3">Maintenance Schedule</h3><table class="min-w-full text-sm" data-table><thead><tr class="text-left border-b"><th class="py-2">Vehicle</th><th>Date</th><th>Type</th><th>Status</th></tr></thead><tbody>@foreach(collect($rows??[]) as $r)<tr class="border-b"><td class="py-2">{{ $r->vehicle ?? $r['vehicle'] ?? '-' }}</td><td>{{ $r->date ?? $r['date'] ?? '-' }}</td><td>{{ $r->type ?? $r['type'] ?? '-' }}</td><td>{{ $r->status ?? $r['status'] ?? '-' }}</td></tr>@endforeach</tbody></table><div data-table-pagination class="text-xs text-slate-500 mt-2"></div></div>