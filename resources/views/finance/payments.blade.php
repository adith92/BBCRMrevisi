<div class="overflow-auto"><table class="min-w-full text-sm"><thead><tr class="text-left border-b"><th class="py-2">Payment#</th><th>Invoice</th><th>Amount</th><th>Date</th></tr></thead><tbody>@foreach(($payments ?? collect()) as $p)<tr class="border-b"><td class="py-2">{{ $p->payment_number ?? '-' }}</td><td>{{ $p->invoice->invoice_number ?? '-' }}</td><td>{{ formatIDR($p->amount ?? 0) }}</td><td>{{ $p->paid_at ?? '-' }}</td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ method_exists(($payments ?? null),'links') ? $payments->links() : '' }}</div>